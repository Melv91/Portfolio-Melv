function scrollToSection() {
    // Trouve la section
    const section = document.getElementById('section2');
    
    // Défile jusqu'à la section avec un effet fluide
    window.scrollTo({
      top: section.offsetTop,
      behavior: 'smooth'
    });
  }


  function scrollToSection2() {
    // Trouve la section
    const section = document.getElementById('section3');
    
    // Défile jusqu'à la section avec un effet fluide
    window.scrollTo({
      top: section.offsetTop,
      behavior: 'smooth'
    });
  }


  function scrollToSection3() {
    // Trouve la section
    const section = document.getElementById('sectionHome');
    
    // Défile jusqu'à la section avec un effet fluide
    window.scrollTo({
      top: section.offsetTop,
      behavior: 'smooth'
    });
  }

  function scrollToSection4() {
    // Trouve la section
    const section = document.getElementById('section5');
    
    // Défile jusqu'à la section avec un effet fluide
    window.scrollTo({
      top: section.offsetTop,
      behavior: 'smooth'
    });
  }

  function scrollToSection5() {
    // Trouve la section
    const section = document.getElementById('section4');
    
    // Défile jusqu'à la section avec un effet fluide
    window.scrollTo({
      top: section.offsetTop,
      behavior: 'smooth'
    });
  }


  